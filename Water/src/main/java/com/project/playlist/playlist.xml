<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="playlist">
	<!-- <insert id="insertplist" parameterType="pdto">
	insert into playlist values (seq_pnum.nextval,#{userid, jdbcType=VARCHAR},#{artist, jdbcType=VARCHAR},#{title, jdbcType=VARCHAR},#{youurl, jdbcType=VARCHAR})
	</insert> -->
	<insert id="insertplist" parameterType="pdto">
	insert into playlist values (seq_pnum.nextval,#{userid, jdbcType=VARCHAR},#{artist, jdbcType=VARCHAR},#{title, jdbcType=VARCHAR},#{youurl, jdbcType=VARCHAR})
	</insert>
	<!-- <select id="selectplist" parameterType="string" resultType="pdto">
	select * from playlist where userid=#{userid}
	</select> -->
</mapper>